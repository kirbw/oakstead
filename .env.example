OAKSTEAD_DATA_DIR=
OAKSTEAD_DEFAULT_HOST=127.0.0.1
OAKSTEAD_DEFAULT_PORT=3000
OAKSTEAD_UPDATE_MODE=git
# DEMO_MODE=1 disables all authentication (every visitor is treated as admin). Use only for throwaway public demos, never with real student data.
DEMO_MODE=0
DEMO_REFRESH_HOURS=2
